[pytest]
# The repository root on sys.path, so tests import `src` the same way every
# script and the Docker image do. One import path, no per-test sys.path edits.
pythonpath = .
testpaths = tests
addopts = -ra
filterwarnings =
    error::DeprecationWarning:src.*
